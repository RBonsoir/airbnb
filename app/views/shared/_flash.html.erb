<% flash.select { |type| %i(danger notice success).include?(type[0].to_sym) } .each do |type, message| %>
  <div class="alert alert-<%= type %> fade in">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>
